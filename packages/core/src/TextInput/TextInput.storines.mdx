import { Story, Canvas, Title,  ArgsTable } from "@storybook/addon-docs";
import TextInput from './index';

<Meta title="Form Controls/TextInput" component={TextInput} />

export const Template = (args) => <Checkbox {...args} />;

<Title />

const Template = (args) => <TextInput {...args} />;



## Intent

Intents provide a way to visually communicate the status of an input. For example, a `TextInput` with a `intent="danger"`
could communicate an error state while a `TextInput` with a `intent="success"` might indicate a validated input.

<Canvas>
    <Story
        name="Intent:Danger"
    >
        {Template.bind({})}
    </Story>
    <Story
        name="Intent:Success"
        args={{
            intent: 'success',
            placeholder: 'placeholder',
        }}
    />
    <Story
        name="Intent:Success"
        args={{
            intent: 'success',
            placeholder: 'placeholder',
        }}
    />
</Canvas>


<ArgsTable />

<!--
<Story id="textinput--intent-danger" />
<Story id="textinput--intent-success" />
<Story id="textinput--intent-warning" />
<Story id="textinput--intent-primary" /> -->

 Title,
  Subtitle,
  Description,
  Primary,
,